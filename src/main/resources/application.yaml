server:
  port: 8070

spring:
  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/test_board?serverTimezone=UTC&characterEncoding=utf-8
      username: root
      password: shdyd0509!

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
